import React from 'react';
import ReactDOM from 'react-dom/client';
import Notifications from '../a01029079/components/notifications';

const root = ReactDOM.createRoot(document.getElementById('class7')!);
root.render(
  <React.StrictMode>
    <Notifications />
  </React.StrictMode>
);